a2-1:

-Added icon to .help
-Redesigned icon of Unknown File
-Added icon to .cfg
-Renamed settings -> settings.cfg
-Fixed bug with types of files (lualualualua before is a .lua)
-A little optimised os 
-Fixed bug with open files with names like "RAM test.lua"
-Added a file Settings.lua
-Added icon to Settings.lua
-The development of a program for configuring the system has begun. It doesn't work at the moment.

a2:

-Now version is a variable, not a file
-Changed a logo on taskbar
-Fixed bsod.lua
-Redesigned logo
-Redesigned icon of Unknown File
-Redesigned .lang icon
-Optimised os
-Fixed bug with files, located outside the monitor's borders
-Renamed lang -> settings (not .lang packets)
-Now help files have own type (.help)
-Added settings.help
-Now you can open files in os (Yeah!)
-Optimised progress bar in pgbar.lua
-Added new progress bar (fullbar)
-Renamed fos -> fos.lnk
-Optimised  and redesigned Installer

a1-1:

-added a shortcut on /home
-Now version showing on left upper corner
-Pastebin is Glitching, so i moved installer from Pastebin to GitHub

a1:

-added help in shell
-added debug lib
-added System Logo
-Fixed Visual Bug in pgbar lib
-Now version showing on right bottom corner